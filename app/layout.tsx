import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Su Indra Groups Pvt. Ltd. | Secure & Scalable Digital Solutions",
  description: "A leading venture company in Nepal specializing in Enterprise Web Solutions, E-Commerce Infrastructure, and Cybersecurity.",
  keywords: ["Su Indra Groups", "IT Company Nepal", "BuyNow Nepal", "Cybersecurity Nepal", "Venture Capital Nepal"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
